import { describe, it, expect } from "vitest";

// Exemplu de test unit. Înlocuiește cu teste pe logica ta critică
// (ex. calcule de termene, validări — acolo testele sunt scut de răspundere).
describe("example", () => {
  it("adună corect", () => {
    expect(1 + 1).toBe(2);
  });
});
